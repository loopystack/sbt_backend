import React from "react";

export default function Dashboard() {
    return (
      <section className="space-y-6">
        <h1 className="text-xl font-semibold">Dashboard</h1>
        <p>Favorites, alerts, and recent activity.</p>
      </section>
    );
  }
  